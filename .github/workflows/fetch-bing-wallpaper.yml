name: Fetch Bing Wallpaper Daily

on: 
  workflow_dispatch
#  schedule:
#    - cron: '15 2 * * *'

jobs:
  fetch-bing-wallpaper:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::group::🏃 Runner start up"
          echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
          echo "🔎 The branch name is ${{ github.ref }} and the repository is ${{ github.repository }}."
          echo "::endgroup::"
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip' # caching pip dependencies
      - run: |
          python3 -m pip install --upgrade pip
          pip install requests
          pip install httpcore
          cd ${{ github.workspace }}/wallpaper
          python3 ./bing.py
      - uses: scapeville/action-git-add-all-then-commit-then-push@v1
        with:
          msg: $COMMIT_MSG
        env:
          GH_TOKEN: ${{ github.token }}
          # $(date +'...') Usage: https://manpages.debian.org/bullseye/coreutils/date.1.en.html
          COMMIT_MSG: Update by Github action $GITHUB_WORKFLOW on $(date +'%Y-%m-%d') # Run No.$GITHUB_RUN_NUMBER
      - run: echo "🍏 This job's status is ${{ job.status }}."
